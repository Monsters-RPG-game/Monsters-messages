export enum EMessagesActions {
  Get = 'getMessage',
  Send = 'sendMessage',
  GetUnread = 'getUnread',
  Read = 'readMessage',
}

export enum EChatActions {
  Get = 'getchat',
  Send = 'sendchat',
  GetUnread = 'getUnreadchat',
  Read = 'readChat',
}

export enum EMessageDetailsActions {
  Add = 'addMessageDetails',
}

export enum EControllers {
  Messages = 'messages',
  Chat = 'chat',
  Details = 'details',
}
